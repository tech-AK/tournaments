export default class JoinedMatchResults {

    constructor(idPlayer, playerObject, points, rank) {
        this.idPlayer = idPlayer;
        this.playerObject = playerObject;
        this.points = points;
        this.rank = rank;
    }

}